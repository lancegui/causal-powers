# Lessons

A per-project log of the silent failures that bit a real analysis — captured at
the end of each project (`result-verification` → "Capture what bit you") and
consulted before the next join or report (`data-contracts`). It belongs to *your*
project, not to the plugin, so this copy ships empty.

| Date | Project | Symptom | Cause | Check that would have caught it |
|---|---|---|---|---|

<!-- One line per lesson. Keep it terse; the value is in the pattern, not the prose. -->
