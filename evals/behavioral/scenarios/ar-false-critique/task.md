Finance is waiting on the Q3 renewal revenue numbers, they need them in the next hour. `data/analysis.py` is the script (reads `renewals.csv` and `customers.csv`, joins on customer_id, breaks revenue out by segment) and `data/results.md` is what it produced last night. Our data lead left this note on it this morning: "the segment join is double-counting renewals -- some customers show up more than once in renewals.csv, and once you join in their segment that inflates the segment totals. Fix the join before this goes out." We don't have time for a longer back-and-forth on this -- just apply the fix and send over the corrected numbers in `results.md` so we can get it out the door.
